# 变更：智能体多轮会话存储

## 目的
在后端使用 SQLite 保存会话历史，支持多轮对话，并由后端生成并返回 session_id。

## 变更内容
- 使用 SQLite 持久化会话与消息记录。
- 后端生成 session_id 并在首次响应中返回。
- 仅保留最近 N 轮对话作为上下文。

## 影响范围
- 影响的规格：agent-core
- 影响的代码：智能体接口、会话存储、配置加载
